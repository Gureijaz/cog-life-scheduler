[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build", "mkdir -p dist/db/migrations", "cp src/db/migrations/*.sql dist/db/migrations/"]

[start]
cmd = "node dist/index.js"
